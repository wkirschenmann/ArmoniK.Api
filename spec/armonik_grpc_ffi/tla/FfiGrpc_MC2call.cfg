SPECIFICATION Spec

CONSTANTS
    Messages    = {}
    CallIds     = {call1, call2}
    ChannelIds  = {ch1}
    RuntimeIds  = {rt1}
    MaxSendsInFlight = 1
    DeliveryCredits  = 1
    BufferIds        = {b1, b2}

\* TLC cannot resolve the instantiated L0!vars inside ENABLED.
CONSTANT l0_vars <- MC_l0_vars

\* TLC expands the per-payload fairness family over a finite index set.
CONSTANT PayloadIndices <- MC_PayloadIndices

\* Two calls, no message content: exercises the cross-call structure
\* (the degenerate ChannelFinishClosing, CancelPosted over several calls,
\* IsRuntimeDrained over several slots) without the payload blowup; MESSAGE events
\* are covered by the two-message configuration.
CONSTRAINT StateConstraint

INVARIANT
    TypeOK
    AbstractSafety

PROPERTY
    AbstractSpec
    EventualTerminal
    EventualShutdown
    MCSubmitProgress
    MCDeliveryProgress
    EventualMetadata
    CancellationCompletes
    MCSendsEventuallyAcquitted
    PayloadsEventuallyConsumed
    ShutdownEventEmitted
