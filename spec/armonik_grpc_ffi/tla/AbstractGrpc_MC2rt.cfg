SPECIFICATION Spec

CONSTANTS
    Messages    = {m1}
    CallIds     = {call1}
    ChannelIds  = {ch1}
    RuntimeIds  = {rt1, rt2}

\* Two runtime slots: exercises SingleRuntime, the RuntimeCreate
\* quiescence guard (a second runtime only after the first is RELEASED)
\* and the RemainFailed sole-active-slot stutter.
CONSTRAINT StateConstraint

INVARIANT
    SafetyInvariant

PROPERTY
    EventualTerminal
    EventualShutdown
    MCSubmitProgress
    MCDeliveryProgress
    EventualMetadata
