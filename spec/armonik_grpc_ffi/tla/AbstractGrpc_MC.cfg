SPECIFICATION Spec

CONSTANTS
    Messages    = {m1}
    CallIds     = {call1}
    ChannelIds  = {ch1}
    RuntimeIds  = {rt1}

\* SYMMETRY disabled for liveness checking (TLC warning: symmetry can miss liveness violations)
\* SYMMETRY Symmetry

\* Bound otherwise-unbounded abstract message streams for finite exploration.
CONSTRAINT StateConstraint

INVARIANT
    SafetyInvariant

PROPERTY
    EventualTerminal
    EventualShutdown
    MCSubmitProgress
    MCDeliveryProgress
    EventualMetadata
